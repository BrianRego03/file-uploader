<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>

    <div>
        <% if(user){ %>
            <div>Greetings <%= user.name %>
            </div>
            <a href="/logout">Log out</a>
            <% }else{ %>
                <a href="/signup">Create account</a>
                <a href="/login">Log in</a>
                <% } %>

    </div>
</body>
</html>